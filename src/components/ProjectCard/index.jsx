import React from 'react';

const ProjectCard = () => {
    return (
        <div className="ProjecCard">
            ▪ Developed  an application where  photographers  can post a  pic for  a  given topic.  
            Topic is  posted on  a  weekly  basis,  the photo which  receives  maximum number  of  votes  for  that week  is  declared  as  winner. ▪ Users  can  sign  into  the  application  via  Facebook. ▪ React JS   is  used   for  the  front  end  development 
            and MySQL as  the  database  system. 
        </div>
    )
}